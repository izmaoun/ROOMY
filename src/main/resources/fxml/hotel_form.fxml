<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>
<?import javafx.scene.image.ImageView?>

<ScrollPane fitToWidth="true"
            style="-fx-background: transparent; -fx-border-color: transparent;"
            xmlns="http://javafx.com/javafx/17.0.8"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.roomy.Controller.HotelFormController">

    <VBox spacing="20" alignment="TOP_CENTER"
          style="-fx-padding: 30; -fx-background-color: #F5F0E6;">

        <!-- Titre -->
        <Text fx:id="titleText" text="Ajouter un nouvel hôtel"
              style="-fx-fill: #380F17; -fx-font-size: 36px; -fx-font-weight: bold;"/>

        <!-- Formulaire -->
        <VBox spacing="15" style="-fx-border-color: #A59090; -fx-border-radius: 10; -fx-padding: 20; -fx-background-color: white;">

            <!-- Nom de l'hôtel -->
            <VBox spacing="5">
                <Label text="Nom de l'hôtel *" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
                <TextField fx:id="tfNomHotel" promptText="Ex: Hotel Marrakech"
                           style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
            </VBox>

            <!-- Nombre d'étoiles -->
            <VBox spacing="5">
                <Label text="Nombre d'étoiles *" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
                <ComboBox fx:id="cbEtoiles" promptText="Sélectionner"
                          style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
            </VBox>

            <!-- Adresse - Rue -->
            <VBox spacing="5">
                <Label text="Rue *" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
                <TextField fx:id="tfRue" promptText="Ex: Boulevard Mohammed V"
                           style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
            </VBox>

            <!-- Adresse - Ville -->
            <VBox spacing="5">
                <Label text="Ville *" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
                <TextField fx:id="tfVille" promptText="Ex: Marrakech"
                           style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
            </VBox>

            <!-- Adresse - Code postal -->
            <VBox spacing="5">
                <Label text="Code postal *" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
                <TextField fx:id="tfCodepostal" promptText="Ex: 40000"
                           style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
            </VBox>

            <!-- Adresse - Pays -->
            <VBox spacing="5">
                <Label text="Pays *" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
                <TextField fx:id="tfPays" promptText="Ex: Maroc"
                           style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
            </VBox>

        </VBox>

        <!-- Description de l'hôtel -->
        <VBox spacing="5">
            <Label text="Description" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>
            <TextArea fx:id="taDescription" promptText="Ex: Un magnifique hôtel 5 étoiles..."
                      wrapText="true" prefRowCount="4"
                      style="-fx-padding: 10; -fx-font-size: 12px; -fx-border-radius: 5;"/>
        </VBox>

        <!-- Section Gestion des Images -->
        <VBox spacing="10" style="-fx-border-color: #A59090; -fx-border-radius: 10; -fx-padding: 20; -fx-background-color: white;">
            <Label text="📸 Gestion des images" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #380F17;"/>

            <Button fx:id="btnSelectImage" text="+ Sélectionner une image"
                    prefWidth="200" prefHeight="40"
                    style="-fx-background-color: #2196F3; -fx-text-fill: white;
                           -fx-font-size: 12px; -fx-font-weight: bold; -fx-background-radius: 8;
                           -fx-cursor: hand;"/>

            <Label fx:id="lblImageStatus" text="Aucune image sélectionnée"
                   style="-fx-font-size: 11px; -fx-text-fill: #666;"/>

            <ImageView fx:id="imgPreview" fitWidth="150" fitHeight="120"
                       preserveRatio="true"
                       style="-fx-border-color: #ddd; -fx-border-radius: 5;"/>

            <ScrollPane fx:id="scrollImages" prefHeight="200" style="-fx-background: transparent; -fx-border-color: transparent;">
                <VBox fx:id="imagesContainer" spacing="10" style="-fx-padding: 10;">
                </VBox>
            </ScrollPane>
        </VBox>

        <!-- Messages d'erreur/succès -->
        <Label fx:id="lblMessage" text=""
               style="-fx-font-size: 12px; -fx-text-fill: #d32f2f;"
               wrapText="true"/>

        <!-- Boutons -->
        <HBox spacing="15" alignment="CENTER">
            <Button fx:id="btnSave" text="💾 Enregistrer"
                    prefWidth="150" prefHeight="45"
                    style="-fx-background-color: #4CAF50; -fx-text-fill: white;
                           -fx-font-size: 14px; -fx-font-weight: bold; -fx-background-radius: 8;
                           -fx-cursor: hand;"/>
            <Button fx:id="btnCancel" text="❌ Annuler"
                    prefWidth="150" prefHeight="45"
                    style="-fx-background-color: #A59090; -fx-text-fill: white;
                           -fx-font-size: 14px; -fx-font-weight: bold; -fx-background-radius: 8;
                           -fx-cursor: hand;"/>
            <Button fx:id="btnDelete" text="🗑️ Supprimer"
                    prefWidth="150" prefHeight="45"
                    visible="false" managed="false"
                    style="-fx-background-color: #f44336; -fx-text-fill: white;
                           -fx-font-size: 14px; -fx-font-weight: bold; -fx-background-radius: 8;
                           -fx-cursor: hand;"/>
        </HBox>

    </VBox>

</ScrollPane>